{
  "name": "<%= name %>",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=6.10.0"
  },
  "description": "<%= name %>",
  "main": "<%= name %>.js",
  "scripts": {
    "build": "gulp build",
    "start": "gulp watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
  },
  "devDependencies": {
  }
}
